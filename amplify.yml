version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site build completed"
  artifacts:
    # public/ 폴더만 배포 (테스트 파일, server.js, Apps Script 코드 등은 공개되지 않음)
    baseDirectory: public
    files:
      - '**/*'
